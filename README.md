


```
. client
    clone https://github.com/GaetanDesrues/ReactBoilerplate
    run `npm install` to install the basic dependencies
    run `npm install @mui/material @emotion/react @emotion/styled` (see https://mui.com/)
. server
    python -m venv venv
    source venv/bin/activate
    pip install aiohttp aiohttp_cors
```