import {Container, Typography} from "@mui/material";

export default function HomePage() {
	return (
		<Container>
			<Typography variant="h1" align="center">
				Hello
			</Typography>

			<Typography align="center">
				See all the examples in the menu
			</Typography>
		</Container>
	);
}